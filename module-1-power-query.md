# Part 1. 데이터를 Tableau가 읽을 수 있는 형태로 정제하기

---

# Part 1-1. 왜 Power Query를 써야 하는가

---

## 1. 이번 장에서 배우는 것
- 데이터 전처리가 왜 시각화보다 먼저 선행되어야 하는지 설명할 수 있다.
- Power Query가 엑셀의 한계를 보완하는 방식을 이해한다.
- 내가 가진 데이터가 분석 가능한 상태인지 스스로 점검할 수 있다.

---

## 2. 왜 중요한가

{% hint style="info" %}
💼 **실무 사례:** [가상 시나리오] 마케팅팀 담당자가 세 개 매체의 광고 성과 파일을 각각 받았다. 파일마다 날짜 형식이 달랐고("2024.11" vs "11/2024"), 열 이름도 제각각이었다. 이 상태로 Tableau에 연결하자 차트가 오작동했다.
{% endhint %}
- **PAST**: 엑셀에서 데이터를 복붙하거나 수작업으로 정리하다 실수가 반복되고, 다음 달 데이터가 오면 처음부터 다시 해야 한다.
- **PRESENT**: 지금 가진 데이터가 Tableau에서 제대로 읽히지 않거나 집계 결과가 틀리는 이유는 대부분 데이터 구조 문제다.
- **FUTURE**: Power Query 파이프라인을 한 번 세우면, 다음 달 데이터가 업데이트되어도 버튼 하나로 재정제된다.
- **BRIDGE**: 이 장은 그 파이프라인의 목적과 구조를 이해하는 출발점이다.

[DATA_REQUIRED] Power Query 미사용 시 데이터 정제 평균 소요 시간 관련 실무 통계

---

## 3. 핵심 개념

### 개념 1. 전처리는 분석의 선행 조건이다
- **정의:** 날것의 데이터(자료)를 필터, 수정, 결합의 가공을 거쳐 인사이트 도출이 가능한 상태(정보)로 만드는 행위다.
- **실무적 의미:** 전처리가 실패하면 분석 결과도 실패한다. "Garbage In, Garbage Out"이다.
- **실무 포인트:** 시각화 도구에 전처리 부하를 전가하면 대시보드 속도가 저하되고 유지보수가 어려워진다. 전처리는 반드시 시각화 도구 바깥에서 처리한다.

### 개념 2. Power Query란?
- **정의:** 엑셀과 Power BI에 내장된 마이크로소프트의 데이터 변환 도구로, 마우스 클릭만으로 데이터를 가져오고 정제하는 기능을 제공한다.
- **실무적 의미:** SQL의 `SELECT`, `JOIN`, `WHERE`와 동등한 작업을 코딩 없이 수행한다. 수행한 모든 작업은 `적용된 단계(Applied Steps)`에 순서대로 기록되어, 원본 파일이 바뀌어도 동일한 처리를 자동 재실행한다.
- **실무 포인트:** 엑셀 100만 행 표시 한계를 넘어 백그라운드에서 동작하며, 대용량 로그 데이터 처리에 적합하다.

---

## 4. 따라하기

### 실습 목표
현재 보유한 데이터가 바로 분석 가능한 상태인지 점검 기준을 세운다. (1-1장은 이론 중심 챕터다. 소프트웨어 실습보다 분석 환경에 대한 목적의식 점검에 집중한다.)

### 준비 데이터
개별 현업 샘플 데이터 또는 [DATA_PLACEHOLDER: 실습 데이터셋].

#### Step 1. 내 데이터의 상태를 진단한다
- 현재 사용하려는 데이터 파일을 엑셀로 열어 다음 항목을 확인한다.
  - 날짜 열의 형식이 통일되어 있는가? (예: `2024-01-15` 형식 일관성)
  - 숫자 열에 텍스트나 특수문자가 섞여 있지 않은가? (예: `1,200원`, `$200`)
  - 빈 행이나 헤더가 여러 줄로 되어 있지 않은가?

#### Step 2. 점검 결과를 체크리스트에 기록한다
- 위 항목 중 하나라도 문제가 있는 경우, 해당 항목에 표시를 남긴다.
- 이 표시가 곧 Power Query에서 처리해야 할 작업 목록이 된다.

### 실습 결과 확인
자신의 데이터에서 결측값이나 이상값이 있는지 확인하는 질문을 스스로 할 수 있게 된다면 이 장의 학습 흐름을 올바르게 따라온 것이다.

---

## 5. 실무 적용 예시

{% hint style="info" %}
💼 **실무 사례:** [가상 시나리오] 마케팅 에이전시에서 네이버, 메타, 구글 광고 성과를 각각 엑셀로 받는다. 파일마다 날짜 포맷과 열 이름이 다르다. Power Query를 통해 날짜 포맷을 통일하고 세 파일을 세로로 합친 뒤 Tableau로 넘기면, 이후 매달 파일만 교체하면 동일한 처리가 자동 적용된다.
{% endhint %}
---

## 6. 자주 하는 실수

{% hint style="warning" %}
⚠️ **실수:** "Tableau에서 다 바꿔줄 텐데 엑셀에서 할 필요 없지 않나?"
**해결:** Tableau 내부에서도 정제 기능이 있으나, 시각화 도구에 전처리 부하를 전가하면 대시보드 속도가 저하된다. 전처리는 반드시 Power Query에서 처리하고 정제된 데이터만 Tableau로 넘긴다.
{% endhint %}
{% hint style="warning" %}
⚠️ **실수:** 데이터를 확인하지 않고 곧바로 Tableau에 연결한다.
**해결:** Tableau에 연결하기 전 반드시 원본 데이터의 열 유형, 결측값, 날짜 형식을 육안으로 점검한다.
{% endhint %}
---

## 7. 실무 체크리스트

{% hint style="success" %}
✅ **체크포인트**

- [ ] 현재 사용하려는 데이터의 결측치 및 형식 오류를 인지하고 있는가?
- [ ] 시각화 도구로 넘기기 전 데이터 용량을 줄여야 하는지 판단할 수 있는가?
- [ ] 전처리와 시각화의 역할 분담을 설명할 수 있는가?
{% endhint %}
---

## 8. 연습 문제

### 문제 1.
데이터 시각화를 하기 전, 엑셀 파일에서 불필요한 공백을 지우거나 다른 표를 붙여 구조를 바꾸는 모든 과정을 한 단어로 무엇이라 부르는가?

{% hint style="info" %}
🔽 **정답 보기**
데이터 전처리 (Data Preprocessing)
{% endhint %}
---

## 9. 요약
- 전처리가 실패하면 분석 결과도 실패한다. 시각화 이전에 데이터 상태를 점검하는 것이 필수다.
- Power Query는 코딩 없이 데이터를 정제하고, 처리 과정을 자동 재실행하는 핵심 도구다.
- 전처리 작업은 Tableau가 아닌 Power Query에서 처리한다.

---

## 10. 다음 장 연결

이 장에서 전처리의 목적을 이해했다. 다음 장(1-2)에서는 Power Query 편집기를 직접 열어 원본 파일을 불러오고 필요한 열만 선택하는 기본 조작을 실습한다.

---

# Part 1-2. 데이터 가져오기와 필요 열 선택

---

## 1. 이번 장에서 배우는 것
- Power Query 편집기를 열어 엑셀 파일을 연결하는 방법을 실행할 수 있다.
- `적용된 단계(Applied Steps)` 패널의 역할과 작업 취소 방법을 이해한다.
- `열 선택(Choose Columns)` 기능으로 불필요한 열을 제거할 수 있다.

---

## 2. 왜 중요한가

- **PAST:** 엑셀 파일을 그냥 열면 100개의 열이 전부 로드된다. 분석에 필요한 열은 5개뿐인데, 나머지 95개가 처리 속도를 잡아먹는다.
- **PRESENT:** Power Query의 `[로드]` 버튼과 `[데이터 변환]` 버튼은 전혀 다른 결과를 만든다. 차이를 모르면 편집기 진입조차 못 한다.
- **FUTURE:** 이 장을 마치면 파일을 열자마자 필요한 열만 남겨 가볍게 만드는 흐름이 손에 익는다.
- **BRIDGE:** `적용된 단계` 패널이 Power Query 자동화의 뼈대임을 이해하는 것이 이 장의 핵심이다.

---

## 3. 핵심 개념

### 개념 1. 적용된 단계 (Applied Steps)
- **정의:** Power Query 편집기 우측에 위치한 패널로, 사용자가 수행한 모든 데이터 조작이 순차적으로 기록되는 이력 창이다.
- **실무적 의미:** `Ctrl+Z` 실행 취소가 작동하지 않는다. 대신 단계 목록 왼쪽의 `X` 버튼을 눌러 해당 작업을 삭제한다. 원본 파일이 바뀌면 `모두 새로고침` 버튼 한 번으로 모든 단계가 재실행된다.
- **실무 포인트:** 단계마다 배경에서 M 언어 코드가 자동 생성된다. 이 코드가 전처리 자동화의 실체다.

### 개념 2. 열 선택 (Choose Columns)
- **정의:** 분석에 불필요한 열을 제거하여 데이터를 경량화하는 기능이다.
- **실무적 의미:** SQL의 `SELECT 컬럼명`과 동일하다. 100개의 열 중 5개만 선택하면 로드 속도와 메모리 사용량이 대폭 감소한다.
- **실무 포인트:** 제거한 열은 `적용된 단계`에서 해당 단계를 삭제하면 언제든 복원할 수 있다.

---

## 4. 따라하기

### 실습 목표
엑셀 파일을 Power Query로 불러와, 실수로 수정한 사항을 되돌리고, 불필요한 열을 제거하여 가벼운 테이블을 만든다.

### 준비 데이터
- 파일명: `Sample-Superstore_Orders.xlsx`
- 주요 필드: 주문번호, 주문일자, 고객ID, 카테고리, 제품명, 매출, 수익

#### Step 1. Power Query 편집기 진입
- 엑셀 상단 메뉴 **[데이터]** 탭으로 이동한다.
- **[데이터 가져오기]** 아이콘을 클릭하고 준비된 `.xlsx` 파일을 선택한다.
- 팝업 `탐색 창`에서 불러올 시트를 클릭한 뒤, 반드시 하단 **[데이터 변환]** 버튼을 누른다. (`[로드]` 버튼을 누르면 편집기가 열리지 않고 데이터가 직접 시트에 덤프된다.)

#### Step 2. `적용된 단계` 동작 확인
- 편집기에 올라온 표에서 아무 열 제목이나 더블클릭하여 이름을 `테스트이름`으로 변경한다.
- 우측 `적용된 단계` 패널 최하단에 **[이름이 지정된 열]** 항목이 생성되었는지 확인한다.
- 해당 항목 왼쪽의 빨간 `X` 버튼을 눌러 원래 이름으로 복구한다.

[IMAGE: 적용된 단계 패널 — 단계 추가/삭제 흐름 | 출처: 자체 제작 | 크기: half]
#### Step 3. 필요한 열만 남기기
- 상단 **[홈]** 탭의 **[열 선택]** 아이콘을 클릭한다.
- 팝업 체크박스 목록에서 **주문일자, 카테고리, 매출, 수익** 4개만 체크하고 나머지는 해제한다.
- **[확인]**을 눌러 경량화된 테이블을 완성한다.

### 실습 결과 확인
- 우측 `적용된 단계` 패널에 `[제거된 다른 열]` 이력이 등록되어 있다면 정상이다.
- [IMAGE: 열 선택 후 경량화된 테이블 결과 | 출처: 자체 제작 | 크기: full]
- ---

## 5. 실무 적용 예시

{% hint style="info" %}
💼 **실무 사례:** [가상 시나리오] 데이터 마케터가 매일 아침 50개 이상의 지표가 포함된 GA 로그 파일을 받는다. 핵심 리포트에 쓰는 지표는 `일자`, `유입 매체`, `전환수` 3개뿐이다. `열 선택`으로 3개 열만 추출되도록 설정해 두면, 이후 수십 MB 파일이 들어와도 처리 시간 없이 필요한 데이터만 로드된다.
{% endhint %}
---

## 6. 자주 하는 실수

{% hint style="warning" %}
⚠️ **실수:** `[로드]` 버튼을 눌러 편집기가 열리지 않는다.
**해결:** 데이터를 가져올 때 반드시 `[데이터 변환]` 버튼을 눌러 편집기 창을 먼저 열어야 한다. `[로드]`는 가공 없이 데이터를 시트에 직접 붓는 버튼이다.
{% endhint %}
{% hint style="warning" %}
⚠️ **실수:** 실수했을 때 `Ctrl+Z`를 연타한다.
**해결:** Power Query에서는 `Ctrl+Z`가 작동하지 않는다. `적용된 단계` 패널에서 해당 단계의 `X` 버튼을 눌러 되돌린다.
{% endhint %}
---

## 7. 실무 체크리스트

{% hint style="success" %}
✅ **체크포인트**

- [ ] `[데이터 변환]` 버튼을 눌러 편집기 창으로 진입했는가?
- [ ] 수정한 뒤 되돌리려면 어느 버튼을 눌러야 하는지 알고 있는가?
- [ ] 분석과 관계없는 열을 `열 선택`으로 제거했는가?
{% endhint %}
---

## 8. 연습 문제

### 문제 1.
원본 엑셀 파일에 새로운 행이 추가된 뒤 저장했다. Power Query로 만들어 둔 결과 테이블을 최신 데이터로 갱신하려면 어떤 버튼을 클릭해야 하는가?

> 🔽 **힌트 보기**
> 엑셀 `[데이터]` 탭에 있다.

{% hint style="info" %}
🔽 **정답 보기**
`[데이터]` 탭의 **모두 새로고침 (Refresh All)**
{% endhint %}
---

## 9. 요약
- 데이터를 Power Query로 불러올 때는 반드시 `[데이터 변환]`을 눌러 편집기로 진입한다.
- `적용된 단계`에서 `X` 버튼으로 작업을 취소할 수 있다.
- `열 선택`으로 불필요한 열을 제거하면 처리 속도와 메모리를 절약한다.

---

## 10. 다음 장 연결

불필요한 열을 제거했다면, 다음 장(1-3)에서는 필요 없는 **행**을 필터로 버리고, 데이터를 카테고리별로 묶어 집계하는 `그룹화(Group By)`를 실습한다.

---

# Part 1-3. 필터 조건과 그룹화(Group By)로 집계하기

---

## 1. 이번 장에서 배우는 것
- 필터로 분석에 불필요한 데이터 행을 제거할 수 있다.
- 날짜 기반의 동적 필터를 설정하여 항상 최신 기간 데이터만 추출할 수 있다.
- `그룹화(Group By)`로 수만 건의 데이터를 범주별 요약 표로 압축할 수 있다.

---

## 2. 왜 중요한가

- **PAST:** 수만 건의 영수증 데이터를 전부 Tableau에 넣으면 차트가 느려지고 불필요한 데이터가 결과를 오염시킨다.
- **PRESENT:** 열을 제거했지만 아직 행(개별 데이터 레코드)의 수는 줄어들지 않았다. 10만 행짜리 파일이 그대로다.
- **FUTURE:** 이 장을 마치면 필요한 기간의 데이터만 남기고, 범주별 집계 요약 표를 만들어 Tableau에 넘길 수 있다.
- **BRIDGE:** 필터와 그룹화로 데이터 볼륨을 극적으로 줄이는 것이 대시보드 성능의 핵심이다.

> 📌 **15분 마이크로 러닝 체크포인트:** 여기까지 따라왔다면 필터로 특정 기간의 데이터를 걸러낼 수 있다.

---

## 3. 핵심 개념

### 개념 1. 필터 (Filter)
- **정의:** SQL의 `WHERE` 구문과 동일하다. 조건에 부합하는 행만 남기고 나머지를 제거한다.
- **실무적 의미:** 날짜 열에는 `올해`, `이번 분기`, `지난달`처럼 동적 시점 조건을 설정할 수 있다. 해가 바뀌어도 파이프라인이 알아서 최신 기간 데이터만 반환한다.
- **실무 포인트:** 동적 날짜 필터를 사용하면 매달 날짜 기준을 수동으로 변경할 필요가 없다.

### 개념 2. 그룹화 (Group By)
- **정의:** 특정 열의 동일한 값을 기준으로 데이터를 묶고, 지정한 집계 함수(합계, 평균, 개수)로 결과를 압축 요약하는 기능이다.
- **실무적 의미:** '의류' 카테고리 5,000행과 '전자제품' 카테고리 2,000행을 `범주별 매출 합계` 형태의 2행짜리 요약 표로 변환한다. Tableau가 처리해야 할 행 수가 7,000에서 2로 줄어든다.
- **실무 포인트:** 측정 가능한 수치(매출, 수익)에만 합계/평균을 적용한다. 식별자(ID, 쿠폰 코드)를 합계로 그룹화하면 의미 없는 결과가 나온다.

---

## 4. 따라하기

### 실습 목표
불필요한 연도 데이터를 필터로 제거하고, 제품 범주별 평균 수익을 보여주는 요약 테이블을 만든다.

### 준비 데이터
- 파일명: `Sample-Superstore_Orders.xlsx` (날짜, 범주, 수익 필드 포함)

#### Step 1. 날짜 기반 필터 적용
- `주문일자` 열 제목 오른쪽 드롭다운 화살표(▼)를 클릭한다.
- **[날짜 필터]** → **[이후...]**를 선택한다.
- 기준 날짜를 `2023-01-01`로 설정하고 확인한다. (2023년 이전 데이터가 모두 제거된다.)

#### Step 2. 기본 그룹화 적용
- 상단 **[홈]** 탭의 **[그룹화]** 아이콘을 클릭한다.
- 그룹화 기준 열: **카테고리**를 선택한다.
- 새 열 이름: `카테고리별 평균 수익`, 연산: **평균**, 열: **수익**으로 설정하고 확인한다.

#### Step 3. 다중 기준 고급 그룹화 (선택 실습)
- 그룹화 창 상단 **[고급]**을 눌러 기준 열을 하나 더 추가한다.
- 기준 열 1: `카테고리`, 기준 열 2: `지역`으로 설정한다.
- 결과를 확인하여 범주×지역 단위 집계가 어떻게 달라지는지 비교한다.

[IMAGE: 그룹화 전(수만 행) vs 그룹화 후(요약 표) 비교 | 출처: 자체 제작 | 크기: full]
### 실습 결과 확인
- 테이블이 범주 수에 해당하는 몇 줄로 압축되었고, 각 범주의 평균 수익 열이 생성되어 있다면 성공이다.

---

## 5. 실무 적용 예시

{% hint style="info" %}
💼 **실무 사례:** [가상 시나리오] HR팀이 전 직원의 일별 경비 지출 내역(100만 행)을 받는다. 본부장 보고에 필요한 것은 `부서별 11월 회식비 합계` 15줄뿐이다. `날짜 필터(지난달)` → `그룹화(부서별 합계)` 파이프라인을 한 번 설정해 두면, 매달 `새로고침` 한 번으로 동일한 요약 표가 자동 생성된다.
{% endhint %}
---

## 6. 자주 하는 실수

{% hint style="warning" %}
⚠️ **실수:** 처음 보는 데이터에 바로 그룹화를 적용한다.
**해결:** 그룹화 전, 어떤 열이 차원(기준)이고 어떤 열이 측정값(숫자)인지 먼저 파악한다. 식별자 열(ID, 주문번호)을 합계로 묶으면 의미 없는 숫자가 도출된다.
{% endhint %}
{% hint style="warning" %}
⚠️ **실수:** 동적 날짜 필터 대신 특정 날짜를 하드코딩한다.
**해결:** `2023-01-01`처럼 고정값으로 지정하면 내년에는 수동으로 기준을 바꿔야 한다. `올해`, `이번 분기` 같은 동적 시점 필터를 사용한다.
{% endhint %}
---

## 7. 실무 체크리스트

{% hint style="success" %}
✅ **체크포인트**

- [ ] 날짜 열에 동적 필터(올해, 이번 분기)를 적용할 수 있는가?
- [ ] 그룹화 결과에서 합계와 평균의 차이가 결과에 어떤 영향을 미치는지 설명할 수 있는가?
- [ ] 고급 그룹화(다중 기준)로 원하는 피벗 요약 표를 만들 수 있는가?
{% endhint %}
---

## 8. 연습 문제

### 문제 1.
그룹화 연산 목록에서 카테고리별 행이 몇 개 있는지 개수를 반환하는 연산의 이름은 무엇인가?

> 🔽 **힌트 보기**
> 행의 개수를 세는 연산이다.

{% hint style="info" %}
🔽 **정답 보기**
**행 수 (Row Count)** — 해당 그룹에 속한 데이터 행의 총 개수를 반환한다.
{% endhint %}
---

## 9. 요약
- 날짜 기반 동적 필터로 분석에 필요한 기간의 데이터만 추출한다.
- 그룹화(Group By)는 수만 건의 데이터를 범주별 요약 표로 압축하는 핵심 기능이다.
- 그룹화 대상은 반드시 측정 가능한 수치 열이어야 한다.

---

## 10. 다음 장 연결

이 장을 마치면 하나의 테이블 안에서 열과 행을 원하는 형태로 가공할 수 있다. 다음 장(1-4)에서는 분리된 두 테이블을 공통 키로 연결하는 **쿼리 병합(Left Join)**을 실습한다.

---

# Part 1-4. 두 테이블 병합하기 (Left Join)

---

## 1. 이번 장에서 배우는 것
- 파워쿼리로 두 개의 분리된 테이블을 공통 키 기반으로 병합할 수 있다.
- Left Join(왼쪽 외부 병합)의 동작 원리와 사용 시점을 설명할 수 있다.
- 병합 후 원하는 열만 확장하여 최종 정제 테이블을 완성할 수 있다.

---

## 2. 왜 중요한가

- **PAST:** 엑셀에서 `VLOOKUP`으로 두 파일의 데이터를 연결하다 파일이 커지면 함수 계산에 수 분이 걸리거나 파일이 먹통이 된다.
- **PRESENT:** 실무 데이터는 보안과 관리 효율을 위해 분산 저장된다. 주문 내역과 고객 등급이 별도 파일에 있는 경우가 일반적이다.
- **FUTURE:** 이 장을 마치면 두 파일을 병합하여 Tableau 시각화에 즉시 사용 가능한 단일 정제 테이블을 완성한다.
- **BRIDGE:** Left Join은 메인 데이터를 보존하면서 보조 정보를 덧붙이는 가장 안전하고 실무 빈도가 높은 병합 방식이다.

> 📌 **30분 마이크로 러닝 체크포인트:** 여기까지 따라왔다면 두 테이블을 Join하여 단일 분석 테이블을 완성할 수 있다.

---

## 3. 핵심 개념

### 개념 1. 쿼리 병합 (Merge Queries)
- **정의:** 두 테이블이 공통으로 보유한 키 열을 기준으로 한 테이블의 정보를 다른 테이블에 가로로 확장 연결하는 작업이다.
- **실무적 의미:** 엑셀 `VLOOKUP`의 데이터베이스 업그레이드 버전이다. `VLOOKUP`은 매 행마다 함수가 재계산되어 대용량에서 느려지지만, 쿼리 병합은 Power Query 엔진이 일괄 처리하므로 안정적이다.
- **실무 포인트:** 기준 키 열의 데이터 유형이 양 테이블에서 반드시 일치해야 한다. 한쪽이 숫자형, 다른 쪽이 텍스트형이면 매칭이 실패한다.

### 개념 2. Left Join (왼쪽 외부 병합)
- **정의:** 왼쪽(기준) 테이블의 모든 행을 유지하고, 오른쪽 테이블에서 키가 일치하는 행만 덧붙이는 방식이다.
- **실무적 의미:** 기준 데이터(주문 내역)를 한 건도 잃지 않으면서 보조 정보(고객 등급)를 붙인다. 오른쪽 테이블에 매칭되는 값이 없으면 해당 셀이 `null`로 표시된다.
- **실무 포인트:** 실무에서 병합의 90% 이상은 Left Join이다. 기준 데이터를 온전히 보존해야 할 때 항상 Left Join을 선택한다.

---

## 4. 따라하기

### 실습 목표
주문 매출 데이터에 별개로 관리되던 `상품별 담당 MD 리스트`를 병합하여, 각 주문 건마다 담당 MD 이름이 한 테이블에 합쳐지도록 가공한다.

### 준비 데이터
- 테이블 A: `[주문 기록]` — `제품ID`, `매출`, `주문일자` 열 포함
- 테이블 B: `[인력 명단]` — `제품ID`, `담당MD명` 열 포함

#### Step 1. 메인 쿼리에서 병합 창 열기
- Power Query 편집기 좌측 쿼리 목록에서 `주문 기록`을 클릭하여 기준 쿼리로 선택한다.
- 상단 **[홈]** 탭의 **[쿼리 병합]** 버튼을 클릭한다.

#### Step 2. 병합 조건 및 조인 종류 설정
- 병합 창 상단 테이블: `주문 기록` (자동 표시됨)
- 하단 드롭다운: `인력 명단`을 선택한다.
- 상단 테이블의 `제품ID` 열을 클릭하고, 하단 테이블의 `제품ID` 열을 클릭한다. (두 열에 번호가 표시되며 키 연결이 설정된다.)
- 조인 종류: **왼쪽 외부(첫 번째 행 전부, 두 번째 행 일치하는 행)**를 선택하고 확인을 누른다.

#### Step 3. 병합된 열 확장하기
- 병합 완료 후 오른쪽 끝에 `[Table]`이라 표시된 새 열이 생성된다.
- 해당 열 제목 오른쪽의 **확장 아이콘**(양방향 화살표)을 클릭한다.
- 팝업에서 필요한 열 **담당MD명**만 체크하고 나머지는 해제한 뒤 확인을 누른다.

[IMAGE: 병합 전 두 테이블 → 병합 후 단일 테이블 구조 비교 | 출처: 자체 제작 | 크기: full]
### 실습 결과 확인
- `주문 기록` 테이블 오른쪽 끝에 `담당MD명` 열이 각 주문 건에 매칭되어 표시된다면 성공이다.
- 매칭 데이터가 없는 행에는 `null`이 표시된다. 이는 정상 동작이다.

> 📌 **모듈 1 최종 산출물:** 이 단계까지 완료되면 `정제 완료 데이터셋.xlsx`를 저장한다. 이 파일이 모듈 2~5 전체 실습의 기반 데이터로 사용된다.

---

## 5. 실무 적용 예시

{% hint style="info" %}
💼 **실무 사례:** [가상 시나리오] 이벤트 발송 성공자(테이블 A)와 고객 등급 데이터(테이블 B)를 `내부 조인(Inner Join)`으로 병합하면, 이벤트 성공 고객 중 데이터베이스에 등록된 우수 고객만 도출할 수 있다. 반면 Left Join을 쓰면 이벤트 성공 고객 전원을 유지하면서 등급 정보만 옆에 붙인다.
{% endhint %}
---

## 6. 자주 하는 실수

{% hint style="warning" %}
⚠️ **실수:** 키 열의 데이터 유형이 다르면 병합이 실패한다.
**해결:** 왼쪽 테이블의 `사원번호`가 숫자형(10488)이고 오른쪽 테이블의 `사원번호`가 텍스트형("10488")이면 매칭이 실패한다. 병합 전 두 테이블의 키 열 유형을 통일한다.
{% endhint %}
{% hint style="warning" %}
⚠️ **실수:** 병합 후 확장 버튼을 누르지 않아 `[Table]` 열 그대로 남겨둔다.
**해결:** 병합 완료 후 반드시 확장 아이콘을 클릭하여 필요한 열을 풀어낸다.
{% endhint %}
---

## 7. 실무 체크리스트

{% hint style="success" %}
✅ **체크포인트**

- [ ] Left Join 사용 시 어느 테이블의 데이터가 전부 보존되는지 설명할 수 있는가?
- [ ] 병합 키 열의 데이터 유형이 양쪽 테이블에서 일치하는지 확인했는가?
- [ ] 병합 후 `[Table]` 열을 확장하여 필요한 정보를 노출했는가?
- [ ] 정제 완료 데이터셋을 저장하고 모듈 2 실습에 사용할 준비가 되었는가?
{% endhint %}
---

## 8. 연습 문제

### 문제 1.
Left Join으로 병합했을 때, 왼쪽 테이블에는 `회원 A`의 구매 기록이 있지만 오른쪽 테이블에는 `회원 A`의 등급 정보가 없다. 병합 후 등급 열의 해당 셀에는 어떤 값이 표시되는가?

{% hint style="info" %}
🔽 **정답 보기**
`null` — 매칭 값을 찾을 수 없을 때 표시된다. Left Join에서 왼쪽 테이블의 행은 보존되지만 오른쪽 값이 없으면 빈 값(null)으로 채워진다.
{% endhint %}
---

## 9. 요약
- 쿼리 병합은 두 테이블의 공통 키 열을 기준으로 정보를 가로로 확장 연결한다.
- Left Join은 기준 테이블의 데이터를 전부 보존하는 가장 안전한 병합 방식이다.
- 병합 기준 열의 데이터 유형은 반드시 양쪽에서 일치해야 한다.

---

## 10. 다음 장 연결

모듈 1을 완료했다. 정제된 데이터셋을 손에 쥔 상태로, 다음 모듈(Part 2)에서는 Tableau를 열기 전 분석 질문을 먼저 정의하는 방법을 배운다.
